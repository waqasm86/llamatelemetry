# Core dependencies
numpy>=1.20.0
requests>=2.31.0
huggingface_hub>=0.20.0
tqdm>=4.65.0

# OpenTelemetry (required for observability core)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# Build dependencies (for pip install from source)
pybind11>=2.10.0
cmake>=3.24

# Optional: OTLP export
# Install with: pip install llamatelemetry[telemetry]
# opentelemetry-exporter-otlp-proto-grpc>=1.20.0
# opentelemetry-exporter-otlp-proto-http>=1.20.0
# opentelemetry-instrumentation>=0.42.0

# Optional: Graph visualization
# Install with: pip install llamatelemetry[graphistry]
# pygraphistry>=0.32.0
# pandas>=1.3.0

# Optional: Jupyter notebook support
# Install with: pip install llamatelemetry[jupyter]
# jupyter>=1.0.0
# ipywidgets>=7.6.0
# matplotlib>=3.5.0

# Development dependencies
# Install with: pip install llamatelemetry[dev]
# pytest>=7.0.0
# pytest-cov>=3.0.0
# black>=22.0.0
# mypy>=0.950
